<%= render 'shared/navbar-signed-in' %>
<div class="container">
  <% if @teacher.formations.empty? == true %>
  <div class="my-formations2 mt-5">
    <h1 style="text-align: center;">Oups, vous n'avez pas de formations</h1>
    <p style="text-align: center; font-size: 50px;">👇</p>
    <div class="oupsy">
  <%= link_to "Créer votre première formation 🙃", new_formation_path, class:"btn btn-primary"%>
  </div>
</div>
  <% else %>
  <div class="my-formations-title">
    <h1 style="text-align: center; font-family: 'Cantarell', sans-serif; color: #142850;" class="title-my-formations mb-5 mt-5">Mes formations <%= image_tag current_teacher.avatar, witdh: 60, height: 60, class: "rounded-circle" %></h1>
  </div>
  <div class="my-formations">
    <div class="my-formations-card1">
      <% @teacher.formations.each do |formation| %>
      <%= render 'shared/my-formations-cards', formation: formation %>
      <% end %>
      <% end %>
    </div>
  </div>
</div>
